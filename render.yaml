
services:
  - type: web
    name: vcnr-secure-backend
    env: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /api/health
